@model EditPermissionsViewModel
@{
    ViewBag.Title = "Sửa quyền hạn";
}
@Html.Partial("_PermissionNavButtons")

<div class="panel">
    <div class="panel-heading">
        <h5>@ViewBag.Title</h5>
    </div>
    <div class="panel-content">
        @using (Html.BeginForm())
        {
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.AntiForgeryToken()

            <div class="form-group">
                @Html.LabelFor(model => model.Name)
                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" + (Model.Lock ? " display" : "") } })
                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.PermissionId)
                @Html.DropDownListFor(model => model.PermissionId, Model.Permissions, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.PermissionId)
            </div>

            if (!Model.Lock)
            {
                <div class="submit-holder">
                    <input type="submit" class="btn-mvc-green btn-mvc-large" value="Cập nhật" />
                </div>
            }
        }
    </div>
</div>

