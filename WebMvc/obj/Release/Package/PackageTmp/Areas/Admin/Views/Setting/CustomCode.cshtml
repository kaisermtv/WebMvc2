@model  CustomCodeViewModels
@{
    ViewBag.Title = "Mã tùy chỉnh";
}
<div class="panel">
    <div class="panel-heading">
        <h4 class="panel-title">
            @ViewBag.Title
        </h4>
    </div>
    <div class="panel-body">
        <p>Bạn có thể thêm bất kỳ mã tùy chỉnh nào (chỉ JavaScript, Html và CSS) vào đầu trang và chân trang của trang web bằng cách sử dụng các hộp bên dưới.</p>
        <p>Mã tiêu đề sẽ xuất hiện ngay trước thẻ đóng head và mã chân trang sẽ xuất hiện ngay trước thẻ body đóng. Mã sẽ xuất hiện trên mọi trang của diễn đàn, vì vậy bạn có thể sử dụng mã này cho những thứ như Analytics.</p>
        @using (Html.BeginForm())
        {
            @Html.ValidationSummary(false)
            @Html.AntiForgeryToken()


            <div class="form-group">
                @Html.LabelFor(m => m.CustomHeaderCode)
                @Html.TextAreaFor(m => m.CustomHeaderCode, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.CustomHeaderCode)
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.CustomFooterCode)
                @Html.TextAreaFor(m => m.CustomFooterCode, new { @class = "form-control" })
                @Html.ValidationMessageFor(m => m.CustomFooterCode)
            </div>

            <div class="submit-holder">
                <input type="submit" class="btn-mvc-green btn-mvc-large" value="Lưu cài đặt" />
            </div>
        }

    </div>
</div>
