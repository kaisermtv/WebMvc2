@model  AdminCreateProductAttributeViewModel
@{
    ViewBag.Title = "Create Product Attribute";
}

@Html.Partial("_ProductNavButtons")

<div class="panel">
    <div class="panel-heading">
        <h5>@ViewBag.Title</h5>
    </div>
    <div class="panel-content">
        @using (Html.BeginForm("CreateAttribute", "AdminProduct", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.ValidationSummary(false)
            @Html.AntiForgeryToken()

            @Html.Partial("_AttributeFields", Model)

            <div class="submit-holder">
                <input type="submit" class="btn-mvc-green btn-mvc-large" value="Khởi tạo" />
            </div>
        }
    </div>
</div>

